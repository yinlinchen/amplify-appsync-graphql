## Query.unionSearch.req.vtl
## Objects of type Collection will be stored in the /collection index

#set( $indexPath = "/*/doc/_search" )
{
    "version": "2018-05-29",
    "operation": "GET",
    "path": "$indexPath.toLowerCase()",
    "params": {
        "body": {
        	"query": {
                "match" : {
                    "title" : {
                        "query" : $util.toJson($ctx.args.query)
                    }
                }
            }
        }
    }
}